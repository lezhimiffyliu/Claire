# Core dependencies
python-dotenv>=1.0.0
supabase>=2.0.0  # legacy auth/storage — being retired (see persistence_sqlalchemy)
sympy>=1.12
requests>=2.31.0

# Standard Postgres persistence (Neon in prod, local Postgres in dev)
SQLAlchemy>=2.0,<2.1
alembic>=1.13
psycopg2-binary>=2.9

# Clerk JWT verification (JWKS / RS256)
PyJWT>=2.8
cryptography>=42.0
pymupdf>=1.24.0  # PDF parsing

# LangChain dependencies
langchain>=0.2.0
langchain-core>=0.2.0
anthropic>=0.25.0
langchain-anthropic>=0.1.0
langchain-openai>=0.1.0
langgraph>=0.2.0

# Gemini vision
google-generativeai>=0.5.0
Pillow>=10.0.0

# API dependencies
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.0.0

# QR code generation
qrcode[pil]>=7.4

# Auto-refresh for polling

# Cookie management for persistent sessions

# Development dependencies
pytest>=8.0.0
httpx>=0.26.0  # For testing FastAPI
